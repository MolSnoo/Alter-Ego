const setdisplayname_bot = include('Commands/setdisplayname_bot');

describe('setdisplayname_bot command', () => {
    beforeEach(() => {
        bot = {};
        game = {};
        message = {};
        player = {};
    });
    
    afterEach(() => {
        jest.clearAllMocks();
    });
    
    test('', async () => {});
});