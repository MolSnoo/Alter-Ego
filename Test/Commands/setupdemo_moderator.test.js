const setupdemo_moderator = include('Commands/setupdemo_moderator');

describe('setupdemo_moderator command', () => {
    beforeEach(() => {
        bot = {};
        game = {};
        message = {};
        player = {};
    });
    
    afterEach(() => {
        jest.clearAllMocks();
    });
    
    test('', async () => {});
});