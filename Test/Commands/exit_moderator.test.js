const exit_moderator = include('Commands/exit_moderator');

describe('exit_moderator command', () => {
    beforeEach(() => {
        bot = {};
        game = {};
        message = {};
        player = {};
    });
    
    afterEach(() => {
        jest.clearAllMocks();
    });
    
    test('', async () => {});
});