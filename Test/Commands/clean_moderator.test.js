const clean_moderator = include('Commands/clean_moderator');

describe('clean_moderator command', () => {
    beforeEach(() => {
        bot = {};
        game = {};
        message = {};
        player = {};
    });
    
    afterEach(() => {
        jest.clearAllMocks();
    });
    
    test('', async () => {});
});