const equip_moderator = include('Commands/equip_moderator');

describe('equip_moderator command', () => {
    beforeEach(() => {
        bot = {};
        game = {};
        message = {};
        player = {};
    });
    
    afterEach(() => {
        jest.clearAllMocks();
    });
    
    test('', async () => {});
});