const gesture_moderator = include('Commands/gesture_moderator');

describe('gesture_moderator command', () => {
    beforeEach(() => {
        bot = {};
        game = {};
        message = {};
        player = {};
    });
    
    afterEach(() => {
        jest.clearAllMocks();
    });
    
    test('', async () => {});
});